<!DOCTYPE html>
<html lang="hu">

<head>
    <title>
        <%= locals.title ?? "Quiz" %>
    </title>
    <link rel="stylesheet" href="style.css" />
</head>

<%
try {
  %><%- include(locals.page) %><%
} catch (error) {
  console.error(`A(z) "${locals.page} oldal betöltése sikertelen."\n`, error);
    %><p><%= `Error loading page "${locals.page}"` %></p><%
}
%>


</html>